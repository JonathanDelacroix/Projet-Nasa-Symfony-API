<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Nasa fetch today's image{% endblock %}</title>
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
        <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" />
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}
        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <body>
    <nav class="navbar navbar-light bg-light">
    
        <a class="navbar-brand" href="{{ path('app_home') }}"><span class="material-symbols-outlined">home</span></a>
        <form class="form-inline">
            {% if app.user is defined and app.user is empty %}
                <button type="button" class="btn btn-primary">
                        <a class="nav-link" href="{{ path('connect_google_start') }}">Connexion</a>
                </button>
            {% endif %}
            {% if app.user is defined and app.user is not empty %}
                <button type="button" class="btn btn-primary">
                    <a class="nav-link" href="{{ path('logout') }}">Déconnexion</a>
                </button>
            {% endif %}
        </form>
    </nav>
    {% block body %}{% endblock %}
    </body>
</html>